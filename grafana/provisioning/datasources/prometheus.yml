apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: "http://localhost:9090"
    access: direct
    isDefault: false
    basicAuth: false
    withCredentials: false
    editable: true